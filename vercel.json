{
  "buildCommand": "prisma generate && npm run build",
  "framework": "vite",
  "devCommand": "vite --port 5173",
  "installCommand": "npm install",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 10
    }
  }
}

